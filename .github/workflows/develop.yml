name: "DEV DEPLOY"

on:
  push:
    branches:
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      environment: dev
      aws-assume-role-arn: "<arn-role-aws>"
      aws-region: "<região-aws>"
      aws-statefile-s3-bucket: "<nome-bucket-s3>"
      aws-lock-dynamodb-table: "<nome-tabela-dynamodb>"
